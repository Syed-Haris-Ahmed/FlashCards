<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlashCards" parent="Theme.AppCompat">
        <!-- Customize your light theme here. -->
        <item name="android:colorBackground">@color/app_dark</item>
        <item name="colorPrimary">@color/app_dark</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FlashCards" parent="Base.Theme.FlashCards" />
    <!-- Base application theme. -->
    <style name="Base.Theme.Design.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Design.NoActionBar" parent="Base.Theme.Design.NoActionBar" />

</resources>