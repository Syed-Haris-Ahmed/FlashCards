<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_dark">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:layout_marginEnd="15dp"
        android:background="@color/app_dark"
        android:elevation="4dp"
        app:layout_constraintBottom_toTopOf="@id/recyclerView"
        app:layout_constraintEnd_toStartOf="@+id/edit_deck_button"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/ic_back_button"
        app:title="Deck Title" />

    <Button
        android:id="@+id/edit_deck_button"
        android:layout_width="25sp"
        android:layout_height="25sp"
        android:layout_marginEnd="60sp"
        android:background="@drawable/ic_edit_button"
        app:layout_constraintBottom_toTopOf="@+id/recyclerView"
        app:layout_constraintEnd_toStartOf="@id/sort_by_button"
        app:layout_constraintStart_toEndOf="@id/toolbar"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/sort_by_button"
        android:layout_width="34sp"
        android:layout_height="25sp"
        android:layout_marginEnd="25sp"
        android:background="@drawable/ic_sort_by_name"
        app:layout_constraintBottom_toTopOf="@+id/recyclerView"
        app:layout_constraintEnd_toStartOf="@id/search_cards_button"
        app:layout_constraintStart_toEndOf="@id/edit_deck_button"
        app:layout_constraintTop_toTopOf="parent" />

    <SearchView
        android:id="@+id/search_cards_button"
        android:layout_width="25dp"
        android:layout_height="?attr/actionBarSize"
        android:layout_marginEnd="15sp"
        android:searchIcon="@drawable/ic_search_icon"
        app:layout_constraintBottom_toTopOf="@+id/recyclerView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/sort_by_button"
        app:layout_constraintTop_toTopOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/app_dark"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:spanCount="1"
        tools:listitem="@layout/cards_layout" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:contentDescription="@string/expandable_buttons"
        android:elevation="20dp"
        android:src="@drawable/ic_add_button"
        android:tint="@android:color/white"
        app:backgroundTint="#0E0E0E"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <!-- The options that will expand when the main FAB is clicked -->
    <LinearLayout
        android:id="@+id/expandedOptions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="140dp"
        android:orientation="vertical"
        android:translationY="150dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Add your additional option buttons here -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabOption1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginBottom="16dp"
            android:contentDescription="@string/expanded_button"
            android:src="@drawable/ic_add_button"
            android:tint="@android:color/white"
            app:backgroundTint="#0E0E0E" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabOption2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginBottom="16dp"
            android:contentDescription="@string/expanded_button"
            android:src="@drawable/ic_shuffle_icon"
            android:tint="@android:color/white"
            app:backgroundTint="#0E0E0E" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
